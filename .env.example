APP_ENV=development
APP_PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=ementau_goapp
DB_SSLMODE=disable
DB_DSN=postgres://postgres:admin@localhost:5432/ementau_goapp?sslmode=disable

# Auth (used once the JWT middleware is enabled)
JWT_SECRET=replace-with-a-secure-random-secret
